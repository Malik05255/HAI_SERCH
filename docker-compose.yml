services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: deepsearch
      POSTGRES_USER: deepsearch
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-deepsearch_local}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U deepsearch -d deepsearch"]
      interval: 10s
      timeout: 5s
      retries: 10

  searxng:
    image: searxng/searxng:latest
    restart: unless-stopped
    volumes:
      - ./infra/searxng/settings.yml:/etc/searxng/settings.yml:ro
    ports:
      - "127.0.0.1:8080:8080"
    mem_limit: 768m

  planner:
    image: ghcr.io/ggml-org/llama.cpp:server
    restart: unless-stopped
    command:
      - -hf
      - Qwen/Qwen2.5-0.5B-Instruct-GGUF:Q4_K_M
      - --host
      - 0.0.0.0
      - --port
      - "8082"
      - -c
      - "2048"
      - -np
      - "1"
      - --n-gpu-layers
      - "0"
    volumes:
      - planner_cache:/root/.cache
    mem_limit: 1100m

  vision:
    image: ghcr.io/ggml-org/llama.cpp:server
    restart: unless-stopped
    command:
      - -hf
      - ggml-org/SmolVLM-256M-Instruct-GGUF:Q8_0
      - --host
      - 0.0.0.0
      - --port
      - "8081"
      - -c
      - "2048"
      - -np
      - "1"
      - --no-mmproj-offload
      - --n-gpu-layers
      - "0"
    volumes:
      - vision_cache:/root/.cache
    mem_limit: 1200m

  api:
    build: ./backend
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    mem_limit: 512m

  worker:
    build: ./backend
    restart: unless-stopped
    env_file: .env
    command: ["python", "-m", "app.runtime"]
    volumes:
      - ./data:/data
    depends_on:
      postgres:
        condition: service_healthy
      searxng:
        condition: service_started
      planner:
        condition: service_started
      vision:
        condition: service_started
    mem_limit: 3g

volumes:
  pgdata:
  planner_cache:
  vision_cache:
