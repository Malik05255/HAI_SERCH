name: deep_search
description: البحث العميق - Android and Windows client
publish_to: "none"
version: 0.3.0+3

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  crypto: ^3.0.6
  file_picker: ^10.3.10
  url_launcher: ^6.3.1
  package_info_plus: ^8.3.1
  path_provider: ^2.1.5
  open_filex: ^4.6.0
  flutter_secure_storage: ^9.2.4
  firebase_core: ^4.15.0
  firebase_messaging: ^16.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
